---
const values = [
  [
    {
      ref: '01',
      name: 'Client-Centric Approach',
      description:
        'We design every feature with multi-account management in mind, ensuring seamless client segregation while maintaining your operational efficiency. Our platform adapts to your workflow, not the other way around.',
    },
    {
      ref: '02',
      name: 'Data Integrity',
      description:
        'Maintain complete data separation between client accounts with enterprise-grade security. Our robust permission system and encryption protocols ensure every client\'s information remains confidential and protected.',
    },
  ],
  [
    {
      ref: '03',
      name: 'Continuous Innovation',
      description:
        'We regularly deploy updates that enhance sub-account management capabilities, from bulk campaign deployment to individual account customization. Stay ahead with automated workflow improvements and new integration options.',
    },
    {
      ref: '04',
      name: 'Collaborative Empowerment',
      description:
        'Our tools foster teamwork through shared client account access, role-based permissions, and collaborative campaign planning features. Enable your team to work smarter across multiple client accounts simultaneously.',
    },
  ],
];
---

<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="flex flex-col gap-8 lg:gap-12">
      <h2 class="text-3xl font-medium tracking-tight sm:text-4xl">Core Principles</h2>
      <div class="grid gap-8 lg:grid-cols-2">
        {
          values.map((column, index) => (
            <div class:list={['space-y-8', index === 1 && 'lg:mt-16']}>
              {column.map((value) => (
                <div class="bg-primary-500/10 dark:bg-primary-400/10 flex flex-col gap-12 rounded-3xl px-6 py-8">
                  <p class="text-sm">{value.ref}</p>
                  <div class="flex flex-col gap-4">
                    <h3 class="text-xl font-medium">{value.name}</h3>
                    <p class="text-primary-950/70 dark:text-primary-200/70 text-base sm:text-lg">
                      {value.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>